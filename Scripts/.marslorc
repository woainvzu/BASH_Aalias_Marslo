function bello()
{
    if [ -e /usr/share/terminfo/x/xterm-256color ];
    then
        export TERM='xterm-256color'
    else
        export TERM='xterm-color'
    fi

    export PATH=$HOME/Marslo/.vim/tools/bin:$PATH
    source $HOME/Marslo/.alias_marslo
}
